import ajax from '@api/ajax'

/**
 * transfer submit
 * @param address 
 * @param amount 
 */
const setAmount = (address: string, amount: string) => ajax('success', 0)


export default {
    setAmount
}